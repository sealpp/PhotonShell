[project]
name = "photon-node"
version = "0.1.0"
description = "PhotonShell local WebSocket-to-SSH node"
requires-python = ">=3.11"
dependencies = [
    "websockets>=13,<14",
    "asyncssh>=2.19,<3",
    "cryptography>=43",
    "grpcio-tools>=1.70,<2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["photon"]
