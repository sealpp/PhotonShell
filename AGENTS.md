# Repository Memory

- `me_PhotonShell/` is a private, independently versioned repository.
- Keep its documentation, designs, diagrams, and related project files inside that
  directory.
- Do not expose private repository details in the outer repository.
- Use concise English Conventional Commits in the form `type(scope): summary`.
- Keep the outer repository public and the nested repository private. Commit and
  push them separately from their respective repository roots.
